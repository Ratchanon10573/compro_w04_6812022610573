#include <stdio.h>

void countCall() {
    static int counter = 0; 
    counter++;
    printf("Call Function: Counter = %d\n", counter);
}

int main() {
    printf("Starting function calls...\n");
    countCall(); 
    printf("After first call:\n");
    countCall(); 
    return 0;
}
/*
1. ผลลัพธ์ที่ได้ต่างกันอย่างไร? เพราะเหตุใด?
   - กรณีที่ 1 (static): ค่าของตัวแปร counter จะ **ไม่ถูกรีเซ็ต** ทุกครั้งที่เรียกฟังก์ชัน → ค่าจะเพิ่มขึ้นเรื่อย ๆ (1, 2)
   - กรณีที่ 2 (non-static): ค่าของตัวแปร counter จะ **ถูกรีเซ็ตเป็น 0 ทุกครั้ง** ที่เรียกฟังก์ชัน → ได้ผลลัพธ์เป็น 1 ทุกครั้ง
2. ความแตกต่างของ Non-Static Variable?
   - ตัวแปร non-static จะถูกสร้างใหม่ทุกครั้งเมื่อเข้าสู่ฟังก์ชัน และจะหายไปเมื่อจบการทำงานของฟังก์ชัน → จึงรีเซ็ตค่าใหม่เสมอ
   - ตรงข้ามกับ static ที่สร้างครั้งเดียวและคงอยู่ตลอดการทำงานของโปรแกรม
3. อภิปราย และสรุปผลที่ได้จากการทดลอง:
   - ตัวแปร static ใช้ในกรณีที่ต้องการ “จำค่าเดิม” ข้ามรอบการเรียกฟังก์ชัน เช่น ตัวนับจำนวนครั้ง
   - ตัวแปร non-static เหมาะกับกรณีที่ไม่ต้องการเก็บค่าข้ามรอบ
   - การเข้าใจความแตกต่างช่วยให้เลือกใช้งานให้ถูกตามวัตถุประสงค์ของโปรแกรม
*/
