#include <stdio.h> //คำสั่ง include ขาดเครื่องหมาย # และ <> ต้องเป็น #include <stdio.h>

int main() {
    int a = 10; //ไม่มีเครื่องหมาย ; หลัง int a = 10
    printf("a = %d", a); //ไม่มีเครื่องหมาย ; หลัง printf("a = %d", a)
    return 0;// ไม่มีเครื่องหมาย ; หลัง return 0
}
