#include <stdio.h> 

int main() { // int is keyword, main is indentifer, () and {} is operater

    printf("Hello,\nC Tokens!\n"); // printf is identifer, () is operator, "Hello,\nC Tokens!\n" is constant

    return 0; // return is keyword, 0 is constant, ; is operator
}